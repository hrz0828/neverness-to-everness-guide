---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { newsItems } from "../../data/news";
import { newsEnglish } from "../../data/contentEnglish";

export function getStaticPaths() {
  return newsItems.map((item) => ({ params: { id: item.id }, props: { item } }));
}

const { item } = Astro.props;
const guide = item.guide;
const english = newsEnglish[item.id];
---

<BaseLayout
  title={`${item.title} | 异环攻略`}
  description={item.excerpt}
  ogImage={item.image}
  jsonLd={{
    "@context": "https://schema.org",
    "@type": "NewsArticle",
    headline: item.title,
    datePublished: item.date,
    author: { "@type": "Organization", name: item.author },
    image: item.image
  }}
>
  <article class="mx-auto max-w-4xl">
    <nav class="text-sm text-zinc-500">
      <a href="/" class="hover:text-neon"><span class="lang-zh">首页</span><span class="lang-en">Home</span></a>
      <span class="mx-2">/</span>
      <span><span class="lang-zh">攻略</span><span class="lang-en">Guides</span></span>
      <span class="mx-2">/</span>
      <span class="text-zinc-300"><span class="lang-zh">正文</span><span class="lang-en">Article</span></span>
    </nav>

    <header class="mt-6">
      <p class="text-sm font-semibold text-neon">
        <span class="lang-zh">{item.category || "攻略更新"}</span>
        <span class="lang-en">{english?.category || "Guide Update"}</span>
      </p>
      <h1 class="mt-2 text-3xl font-black leading-tight text-zinc-950 dark:text-white sm:text-5xl">
        <span class="lang-zh">{item.title}</span>
        <span class="lang-en">{english?.title}</span>
      </h1>
      <div class="mt-4 flex flex-wrap gap-3 text-sm text-zinc-500">
        <span><span class="lang-zh">发布时间：</span><span class="lang-en">Published: </span>{item.date}</span>
        <span><span class="lang-zh">作者：{item.author}</span><span class="lang-en">Author: NTE Guide Editorial Team</span></span>
        {item.readTime && <span><span class="lang-zh">阅读时间：{item.readTime}</span><span class="lang-en">Read time: {english?.readTime || item.readTime}</span></span>}
      </div>
    </header>

    <img
      src={item.image}
      alt={`异环攻略配图-${item.title}`}
      class="mt-8 aspect-[16/9] w-full rounded-xl object-cover"
      loading="eager"
    />

    {
      item.gallery?.length ? (
        <section class="mt-6 grid gap-3 sm:grid-cols-2">
          {item.gallery.map((image) => (
            <figure class="overflow-hidden rounded-lg border border-zinc-200 bg-white shadow-sm dark:border-line dark:bg-panel/80 dark:shadow-none">
              <img src={image.src} alt={image.alt} class="aspect-[16/9] w-full object-cover" loading="lazy" />
              {image.caption && (
                <figcaption class="px-3 py-2 text-xs leading-5 text-zinc-500 dark:text-zinc-400">
                  <span class="lang-zh">{image.caption}</span>
                  <span class="lang-en">Guide reference image</span>
                </figcaption>
              )}
            </figure>
          ))}
        </section>
      ) : null
    }

    {
      guide ? (
        <div class="lang-zh mt-8 space-y-8">
          <section class="rounded-lg border border-teal-200 bg-white p-5 shadow-sm dark:border-neon/30 dark:bg-neon/10 dark:shadow-none">
            <p class="text-xs font-semibold uppercase text-teal-600 dark:text-neon">攻略速览</p>
            <p class="mt-3 text-base leading-8 text-zinc-700 dark:text-zinc-100">{guide.overview}</p>
          </section>

          <section>
            <h2 class="text-2xl font-black text-zinc-950 dark:text-white">前置条件与接取方式</h2>
            <div class="mt-4 grid gap-3 md:grid-cols-3">
              {guide.requirements.map((requirement) => (
                <div class="rounded-lg border border-zinc-200 bg-white p-4 shadow-sm dark:border-line dark:bg-panel/80 dark:shadow-none">
                  <p class="text-sm font-semibold text-teal-600 dark:text-neon">{requirement.label}</p>
                  <p class="mt-2 text-sm leading-6 text-zinc-700 dark:text-zinc-300">{requirement.value}</p>
                </div>
              ))}
            </div>
          </section>

          <section>
            <div class="flex flex-wrap items-end justify-between gap-3">
              <h2 class="text-2xl font-black text-zinc-950 dark:text-white">{guide.stepsTitle || "攻略流程"}</h2>
              <p class="text-sm text-zinc-500">{guide.stepsNote || "按顺序执行，避免重复跑图"}</p>
            </div>
            <div class="mt-4 space-y-3">
              {guide.steps.map((step, index) => (
                <div class="flex gap-4 rounded-lg border border-zinc-200 bg-white p-4 shadow-sm dark:border-line dark:bg-panel/80 dark:shadow-none">
                  <div class="grid size-9 shrink-0 place-items-center rounded-md bg-teal-500 text-sm font-black text-white dark:bg-neon dark:text-black">
                    {index + 1}
                  </div>
                  <div>
                    <h3 class="font-bold text-zinc-950 dark:text-white">{step.title}</h3>
                    <p class="mt-1 text-sm font-semibold text-amber dark:text-amber">{step.action}</p>
                    <p class="mt-2 text-sm leading-6 text-zinc-700 dark:text-zinc-400">{step.detail}</p>
                  </div>
                </div>
              ))}
            </div>
          </section>

          <section class="grid gap-4 lg:grid-cols-[1.25fr_0.75fr]">
            <div class="rounded-lg border border-zinc-200 bg-white p-5 shadow-sm dark:border-line dark:bg-panel/80 dark:shadow-none">
              <p class="text-sm font-semibold text-signal">{guide.challengeLabel || "BOSS战"}</p>
              <h2 class="mt-2 text-2xl font-black text-zinc-950 dark:text-white">{guide.boss.name}</h2>
              <p class="mt-3 text-sm leading-7 text-zinc-700 dark:text-zinc-300">{guide.boss.summary}</p>
              <ul class="mt-4 space-y-3">
                {guide.boss.tips.map((tip) => (
                  <li class="flex gap-3 text-sm leading-6 text-zinc-700 dark:text-zinc-300">
                    <span class="mt-2 size-1.5 shrink-0 rounded-full bg-signal" />
                    <span>{tip}</span>
                  </li>
                ))}
              </ul>
            </div>

            <div class="rounded-lg border border-zinc-200 bg-white p-5 shadow-sm dark:border-line dark:bg-panel/80 dark:shadow-none">
              <p class="text-sm font-semibold text-teal-600 dark:text-neon">完成奖励</p>
              <div class="mt-4 flex flex-wrap gap-2">
                {guide.rewards.map((reward) => (
                  <span class="rounded-md border border-teal-200 bg-teal-50 px-3 py-2 text-sm text-zinc-800 dark:border-neon/30 dark:bg-neon/10 dark:text-zinc-100">
                    {reward}
                  </span>
                ))}
              </div>
              <p class="mt-5 text-sm leading-7 text-zinc-700 dark:text-zinc-400">{guide.closing}</p>
            </div>
          </section>

          {item.paragraphs?.length ? (
            <section class="prose max-w-none">
              <h2>完整解析</h2>
              {item.paragraphs.map((paragraph) => <p>{paragraph}</p>)}
            </section>
          ) : null}
        </div>
      ) : (
        <div class="prose lang-zh mt-8 max-w-none">
          {item.paragraphs.map((paragraph) => <p>{paragraph}</p>)}
        </div>
      )
    }

    {
      english ? (
        <div class="lang-en mt-8 space-y-8">
          <section class="rounded-lg border border-teal-200 bg-white p-5 shadow-sm dark:border-neon/30 dark:bg-neon/10 dark:shadow-none">
            <p class="text-xs font-semibold uppercase text-teal-600 dark:text-neon">Guide Snapshot</p>
            <p class="mt-3 text-base leading-8 text-zinc-700 dark:text-zinc-100">{english.overview}</p>
          </section>

          <section>
            <h2 class="text-2xl font-black text-zinc-950 dark:text-white">Requirements and Entry</h2>
            <div class="mt-4 grid gap-3 md:grid-cols-3">
              {english.requirements.map(([label, value]) => (
                <div class="rounded-lg border border-zinc-200 bg-white p-4 shadow-sm dark:border-line dark:bg-panel/80 dark:shadow-none">
                  <p class="text-sm font-semibold text-teal-600 dark:text-neon">{label}</p>
                  <p class="mt-2 text-sm leading-6 text-zinc-700 dark:text-zinc-300">{value}</p>
                </div>
              ))}
            </div>
          </section>

          <section>
            <div class="flex flex-wrap items-end justify-between gap-3">
              <h2 class="text-2xl font-black text-zinc-950 dark:text-white">{english.stepsTitle}</h2>
              <p class="text-sm text-zinc-500">{english.stepsNote}</p>
            </div>
            <div class="mt-4 space-y-3">
              {english.steps.map(([title, action, detail], index) => (
                <div class="flex gap-4 rounded-lg border border-zinc-200 bg-white p-4 shadow-sm dark:border-line dark:bg-panel/80 dark:shadow-none">
                  <div class="grid size-9 shrink-0 place-items-center rounded-md bg-teal-500 text-sm font-black text-white dark:bg-neon dark:text-black">
                    {index + 1}
                  </div>
                  <div>
                    <h3 class="font-bold text-zinc-950 dark:text-white">{title}</h3>
                    <p class="mt-1 text-sm font-semibold text-amber dark:text-amber">{action}</p>
                    <p class="mt-2 text-sm leading-6 text-zinc-700 dark:text-zinc-400">{detail}</p>
                  </div>
                </div>
              ))}
            </div>
          </section>

          <section class="grid gap-4 lg:grid-cols-[1.25fr_0.75fr]">
            <div class="rounded-lg border border-zinc-200 bg-white p-5 shadow-sm dark:border-line dark:bg-panel/80 dark:shadow-none">
              <p class="text-sm font-semibold text-signal">{english.challengeLabel}</p>
              <h2 class="mt-2 text-2xl font-black text-zinc-950 dark:text-white">{english.boss.name}</h2>
              <p class="mt-3 text-sm leading-7 text-zinc-700 dark:text-zinc-300">{english.boss.summary}</p>
              <ul class="mt-4 space-y-3">
                {english.boss.tips.map((tip) => (
                  <li class="flex gap-3 text-sm leading-6 text-zinc-700 dark:text-zinc-300">
                    <span class="mt-2 size-1.5 shrink-0 rounded-full bg-signal" />
                    <span>{tip}</span>
                  </li>
                ))}
              </ul>
            </div>

            <div class="rounded-lg border border-zinc-200 bg-white p-5 shadow-sm dark:border-line dark:bg-panel/80 dark:shadow-none">
              <p class="text-sm font-semibold text-teal-600 dark:text-neon">Rewards</p>
              <div class="mt-4 flex flex-wrap gap-2">
                {english.rewards.map((reward) => (
                  <span class="rounded-md border border-teal-200 bg-teal-50 px-3 py-2 text-sm text-zinc-800 dark:border-neon/30 dark:bg-neon/10 dark:text-zinc-100">
                    {reward}
                  </span>
                ))}
              </div>
              <p class="mt-5 text-sm leading-7 text-zinc-700 dark:text-zinc-400">{english.closing}</p>
            </div>
          </section>

          <section class="prose max-w-none">
            <h2>Full Analysis</h2>
            {english.paragraphs.map((paragraph) => <p>{paragraph}</p>)}
          </section>
        </div>
      ) : null
    }

    <a
      href="/"
      class="mt-10 inline-flex rounded-lg border border-neon/60 px-4 py-2 text-sm font-semibold text-neon transition hover:bg-neon hover:text-black"
    >
      <span class="lang-zh">返回首页</span><span class="lang-en">Back to Home</span>
    </a>
  </article>
</BaseLayout>
